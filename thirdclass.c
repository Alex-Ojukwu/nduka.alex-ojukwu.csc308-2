#include <stdio.h>

/*int main() {

    float accountBalance = 10000;
     float credit = 5000;
     float debit = 2000;
 
     //int newvariable = accountBalance + credit;

     accountBalance += credit; //shortcut for addition
     accountBalance -= debit;
     printf("%.2f\n", accountBalance); /*%i is just a placeholder for the integer,
      so when we are using floatthe placeholder changes to "%f\n"  %.2f to put the float values in 2 decimal places*/ 

int main() {

    int ispaustudent = 1;
    int isadult = 0;

    if (ispaustudent || isadult) {
        printf("you are eligible to vote\n"); }
     else
       { printf("uneligible to vote\n");
    }


    return 0;
}


